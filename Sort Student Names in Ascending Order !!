#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    char names[100][51], temp[51];

    scanf("%d", &N);

    for (int i = 0; i < N; i++) {
        scanf("%s", names[i]);
    }
    for (int i = 0; i < N - 1; i++) {
        for (int j = 0; j < N - i - 1; j++) {
            if (strcmp(names[j], names[j + 1]) > 0) {
                strcpy(temp, names[j]);
                strcpy(names[j], names[j + 1]);
                strcpy(names[j + 1], temp);
            }
        }
    }

    for (int i = 0; i < N; i++) {
        printf("%s\n", names[i]);
    }

    return 0;
}
