#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
     int n;
    scanf("%d",&n);
    int stu[n];
    for(int i=0;i<n;i++){
    scanf("%d",&stu[i]);
    }
    int flag =0;
    for(int i=0;i<n;i++){
        int count =0;
        for(int j=0;j<n;j++){
            if(stu[i]==stu[j]){
                count++;
            }
        }
        if(count==1){
            printf("%d ",stu[i]);
             flag=1;
        }
    }
    if(flag==0){
        printf("No unique elements");
    }
    return 0;
}
