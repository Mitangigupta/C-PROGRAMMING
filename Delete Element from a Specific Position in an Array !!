#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d",&n);
    int stu[n];
    for(int i=0;i<n;i++){
        scanf("%d",&stu[i]);
    }
    int x;
    scanf("%d",&x);
    if(x<1||x>n){
       printf("Invalid position");
    }else{
     for(int i=x-1;i<n-1;i++){
         stu[i]=stu[i+1];
     }
    for(int i=0;i<n-1;i++){
    printf("%d ",stu[i]);
   }
    printf("\n");
    }
    return 0;
}
