#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n1,n2,sum;
    int *ptr1,*ptr2;
    scanf("%d",&n1);
    scanf("%d",&n2);
    ptr1=&n1;
    ptr2=&n2;
    sum= *ptr1 + *ptr2;
    printf("%d",sum);
    return 0;
}
